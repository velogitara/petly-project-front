import TitlePage from "components/TitlePage";

const NotFound = () => {

    return <TitlePage title={"Page not found"} />
}

export default NotFound;

// This component made as ceparate component in case we need to add some images or more text